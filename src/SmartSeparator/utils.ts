export enum EDrivingForce {
  left = "left",
  right = "right"
}

export enum EActions {
  select = "select",
  remove = "remove"
}

export enum ELocation {
  left = "left",
  right = "right"
}

export enum EScaleContext {
  time = "time",
  genome = "genome"
}
